{
  "name": "grab-route-starter",
  "version": "0.1.0",
  "private": true,
  "description": "Standalone starter app for visualising a start-to-end route before adding Grab API features.",
  "scripts": {
    "dev": "node server.js",
    "start": "node server.js",
    "check": "node --check server.js && node --check public/app.js"
  },
  "engines": {
    "node": "22.x"
  },
  "dependencies": {
    "maplibre-gl": "^4.7.1"
  }
}
